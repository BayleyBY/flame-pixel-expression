<Setup><Base><Version>21.020000</Version><NAME>noise_fbm</NAME><Note></Note><Expanded>False</Expanded><ScrollBar>0</ScrollBar><Frames>0</Frames><Current_Time>1</Current_Time><Input_DataType>4</Input_DataType><ClampMode>0</ClampMode><AdapDegrad>False</AdapDegrad><ReadOnly>False</ReadOnly><NoMediaHandling>1</NoMediaHandling><UsedAsTransition>False</UsedAsTransition><FrameBounds W="1920" H="1080" X="0" Y="0" SX="16" SY="9"/></Base><State><RedExpression>(1.0 * (mix(mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(0.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(1.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0))).x), mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(0.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(1.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0))).x), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0))).y)) + persistence * (mix(mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(0.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(1.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity))).x), mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(0.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(1.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity))).x), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity))).y)) + persistence * persistence * (mix(mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(0.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(1.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity))).x), mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(0.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(1.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity))).x), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity))).y))) / (1.0 + persistence + persistence * persistence)</RedExpression><GreenExpression>(1.0 * (mix(mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(0.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(1.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0))).x), mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(0.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(1.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0))).x), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0))).y)) + persistence * (mix(mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(0.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(1.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity))).x), mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(0.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(1.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity))).x), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity))).y)) + persistence * persistence * (mix(mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(0.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(1.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity))).x), mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(0.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(1.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity))).x), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity))).y))) / (1.0 + persistence + persistence * persistence)</GreenExpression><BlueExpression>(1.0 * (mix(mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(0.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(1.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0))).x), mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(0.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) + vec2(1.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0))).x), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * 1.0))).y)) + persistence * (mix(mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(0.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(1.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity))).x), mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(0.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) + vec2(1.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity))).x), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity))).y)) + persistence * persistence * (mix(mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(0.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(1.0, 0.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity))).x), mix(fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(0.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), fract(sin(dot(floor((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) + vec2(1.0, 1.0), vec2(12.9898, 78.233))) * 43758.5453), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity))).x), (fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity) * (3.0 - 2.0 * fract((vec2(x, y) / scale + vec2(seed, seed * 1.3)) * lacunarity * lacunarity))).y))) / (1.0 + persistence + persistence * persistence)</BlueExpression><MatteExpression>1.0</MatteExpression><RedDeclarations></RedDeclarations><GreenDeclarations></GreenDeclarations><BlueDeclarations></BlueDeclarations><MatteDeclarations></MatteDeclarations><Variables><Variable index="0" name="scale"><Channel Name="scene/noise_fbm/scale"><Extrap>constant</Extrap><Value>80</Value><Uncollapsed/></Channel></Variable><Variable index="1" name="seed"><Channel Name="scene/noise_fbm/seed"><Extrap>constant</Extrap><Value>0.0</Value><Uncollapsed/></Channel></Variable><Variable index="2" name="lacunarity"><Channel Name="scene/noise_fbm/lacunarity"><Extrap>constant</Extrap><Value>2.0</Value><Uncollapsed/></Channel></Variable><Variable index="3" name="persistence"><Channel Name="scene/noise_fbm/persistence"><Extrap>constant</Extrap><Value>0.5</Value><Uncollapsed/></Channel></Variable></Variables><CentreX><Channel Name="scene/noise_fbm/centre/x"><Extrap>constant</Extrap><Value>0</Value><Uncollapsed/></Channel></CentreX><CentreY><Channel Name="scene/noise_fbm/centre/y"><Extrap>constant</Extrap><Value>0</Value><Uncollapsed/></Channel></CentreY><FormulaName0></FormulaName0><FormulaExpression0></FormulaExpression0><FormulaType0>0</FormulaType0><FormulaName1></FormulaName1><FormulaExpression1></FormulaExpression1><FormulaType1>0</FormulaType1><FormulaName2></FormulaName2><FormulaExpression2></FormulaExpression2><FormulaType2>0</FormulaType2><FormulaName3></FormulaName3><FormulaExpression3></FormulaExpression3><FormulaType3>0</FormulaType3></State></Setup>